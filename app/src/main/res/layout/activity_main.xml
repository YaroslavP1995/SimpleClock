<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

    <com.google.android.material.tabs.TabLayout
            android:id="@+id/tab_layout_id"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:minHeight="?actionBarSize"
            app:tabGravity="fill"
            app:tabMode="fixed"
            app:tabTextColor="@color/whiteViewColor"
            app:tabSelectedTextColor="@color/primaryViewColor"
            app:tabIndicatorColor="@color/primaryViewColor"
            app:tabIconTint="@color/primaryViewColor"
            app:tabIndicatorHeight="@dimen/indicator_height_size"
            app:tabBackground="@color/colorPrimary">
    </com.google.android.material.tabs.TabLayout>

    <androidx.viewpager.widget.ViewPager
            android:layout_width="match_parent"
            android:id="@+id/view_pager_id"
            android:layout_height="match_parent"/>
</LinearLayout>